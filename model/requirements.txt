# Core dependencies
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0
scikit-learn>=1.3.0

# Optional: for faster training
# torch with CUDA support should be installed separately:
# pip install torch --index-url https://download.pytorch.org/whl/cu118

